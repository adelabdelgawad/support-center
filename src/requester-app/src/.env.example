# =============================================================================
# Requester App (Tauri + SolidJS) Configuration
# =============================================================================
# Copy this file to .env and update with your server configuration
#
# IMPORTANT: These values are embedded at BUILD TIME
# Changes require rebuilding the application

# =============================================================================
# Network Detection Configuration
# =============================================================================
# The app automatically detects whether to use internal or external DNS
# based on the client's network location (inside or outside corporate network)

# Internal domain (private network DNS - for employees inside the office)
# Used when client IP is within SUPPORTCENTER_PRIVATE_IP_RANGE
VITE_SUPPORTCENTER_DOMAIN_INTERNAL=supportcenter.andalusia.loc

# External domain (public DNS - for employees working remotely/from home)
# Used when client IP is outside SUPPORTCENTER_PRIVATE_IP_RANGE
VITE_SUPPORTCENTER_DOMAIN_EXTERNAL=supportcenter.andalusiagroup.net

# Private IP range for network detection (CIDR notation)
# Determines which domain to use:
# - IP in range (10.x.x.x) → use DOMAIN_INTERNAL (supportcenter.andalusia.loc)
# - IP outside range → use DOMAIN_EXTERNAL (supportcenter.andalusiagroup.net)
VITE_SUPPORTCENTER_PRIVATE_IP_RANGE=10.0.0.0/8

# Protocol configuration
# Use HTTP for API (avoids CORS preflight redirects on internal network)
# Use HTTPS for SignalR (secure WebSocket connections)
VITE_API_PROTOCOL=http
VITE_SIGNALR_PROTOCOL=https

# System Tray Configuration
IS_WINDOWS=true
