/**
 * Remote Access API Client
 *
 * This file previously contained approve/deny functions for the legacy
 * approval dialog flow. Those have been removed as the app now uses
 * the auto-start flow where no approval is needed.
 *
 * Remote access session management is now handled directly via WebRTC.
 */

// This file is kept as a placeholder for future remote access API functions
// that may be needed (e.g., session status checks, analytics, etc.)
