# Docker Compose Environment Variables Example
# ============================================
# Copy this file to .env and update with your actual values
#
# These variables are used by Docker Compose for variable substitution.
# Services reference these using ${VARIABLE_NAME} in their respective
# env files in docker/env/

# ============================================================================
# SHARED SECRETS - Used by multiple services
# ============================================================================

# Redis Configuration (referenced by .env.backend and .env.redis)
REDIS_PASSWORD=your-redis-password-here

# PostgreSQL Database (referenced by .env.postgres and .env.backend)
POSTGRES_DB=servicecatalog
POSTGRES_USER=servicecatalog
POSTGRES_PASSWORD=your-postgres-password-here

# JWT Secret (shared between backend, frontend, and SignalR)
# IMPORTANT: Use a strong, random secret for production!
JWT_SECRET_KEY=your-jwt-secret-key-here-min-32-chars

# Session Secret (shared between backend and frontend for session management)
# IMPORTANT: Use a strong, random secret for production!
SESSION_SECRET=your-session-secret-here-min-32-chars

# TURN Server Secret (shared between coturn service and backend for WebRTC)
# IMPORTANT: Use a strong, random secret for production!
TURN_SECRET=your-turn-secret-here-min-32-chars

# SignalR Internal API Key (referenced by .env.backend and signalr-service)
SIGNALR_INTERNAL_API_KEY=your-signalr-api-key-here

# ============================================================================
# DOCKER COMPOSE OVERRIDES
# ============================================================================

# Service Port Overrides (uncomment to change default ports)
# POSTGRES_PORT=5433
# REDIS_PORT=6380
# FRONTEND_PORT=3010
# SIGNALR_PORT=5000

# SignalR Configuration
# NEXT_PUBLIC_SIGNALR_URL=http://signalr-service:5000/hubs
# NEXT_PUBLIC_API_URL=http://backend-1:8000

# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# ⚠️  NEVER commit the actual .env file with real secrets to git
# ⚠️  Use strong, random passwords for production (min 32 chars recommended)
# ⚠️  Rotate secrets periodically by updating this file
# ⚠️  Keep this file backed up in a secure location
# ⚠️  Use different secrets for development, staging, and production
#
# ============================================================================
