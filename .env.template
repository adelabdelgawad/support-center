# =============================================================================
# SUPPORT CENTER - ROOT ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and update with your actual values
#
# This file contains SHARED SECRETS and DOCKER COMPOSE variables only.
# Service-specific configuration lives in docker/env/.env.* files.
#
# SECURITY NOTES:
# - NEVER commit the actual .env file with real secrets to git
# - Use strong, random passwords for production (min 32 chars recommended)
# - Rotate secrets periodically
# - Use different secrets for development, staging, and production
#
# Generate secure secrets using:
#   openssl rand -hex 32
# =============================================================================


# =============================================================================
# SHARED SECRETS - Used by multiple services via ${VARIABLE} substitution
# =============================================================================

# PostgreSQL Database (referenced by .env.postgres and .env.backend)
POSTGRES_DB=servicecatalog
POSTGRES_USER=servicecatalog
POSTGRES_PASSWORD=CHANGE_ME_POSTGRES_PASSWORD

# Redis (referenced by .env.redis, .env.backend, celery, and signalr)
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# MinIO Object Storage (referenced by .env.minio)
MINIO_ROOT_USER=CHANGE_ME_MINIO_USERNAME
MINIO_ROOT_PASSWORD=CHANGE_ME_MINIO_PASSWORD

# JWT Authentication (shared between backend, frontend, and SignalR)
# CRITICAL: Use a strong, random secret (min 32 chars) for production
JWT_SECRET_KEY=CHANGE_ME_JWT_SECRET_KEY_MIN_32_CHARS

# Session Management (shared between backend and frontend)
SESSION_SECRET=CHANGE_ME_SESSION_SECRET_MIN_32_CHARS

# TURN Server Secret (shared between coturn and backend for WebRTC)
TURN_SECRET=CHANGE_ME_TURN_SECRET_MIN_32_CHARS

# SignalR Internal API Key (referenced by .env.backend and signalr-service)
SIGNALR_INTERNAL_API_KEY=CHANGE_ME_SIGNALR_API_KEY


# =============================================================================
# DOCKER COMPOSE PORT OVERRIDES
# =============================================================================
# Uncomment to change default ports for services

# Infrastructure Ports
# POSTGRES_PORT=5433              # PostgreSQL (default: 5433, container: 5432)
# REDIS_PORT=6380                 # Redis cache (default: 6380, container: 6379)

# Application Ports
# FRONTEND_PORT=3010              # Next.js frontend (default: 3010)
# SIGNALR_PORT=5000               # SignalR real-time service (default: 5000)


# =============================================================================
# SERVICE-SPECIFIC CONFIGURATION
# =============================================================================
# Each service has its own env file in docker/env/:
#
#   docker/env/.env.backend   - Backend API (FastAPI, Celery, MinIO, etc.)
#   docker/env/.env.postgres  - PostgreSQL database
#   docker/env/.env.redis     - Redis cache
#   docker/env/.env.minio     - MinIO object storage
#   docker/env/.env.frontend  - Next.js frontend
#   docker/env/.env.coturn    - CoTURN server (WebRTC)
#
# Copy from docker/env/.env.example.* to docker/env/.env.*
# Those files reference shared secrets above using ${VARIABLE} syntax
# =============================================================================
