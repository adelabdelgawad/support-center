# =============================================================================
# Grafana Configuration (Example/Template)
# =============================================================================
# Copy this file to .env.grafana and update with your actual values
# Never commit .env files with real credentials to version control
#
# Grafana environment variables reference:
# https://grafana.com/docs/grafana/latest/setup-grafana/configure-grafana/

# =============================================================================
# SECURITY - REQUIRED
# =============================================================================
# Admin user credentials
# CRITICAL: Change these in production!
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=CHANGE_ME_GRAFANA_PASSWORD

# Disable user sign-up (recommended for production)
GF_USERS_ALLOW_SIGN_UP=false

# Disable anonymous access (recommended for production)
GF_AUTH_ANONYMOUS_ENABLED=false

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Root URL for Grafana (used for redirects and emails)
# Update with your production domain
GF_SERVER_ROOT_URL=http://localhost:3030
GF_SERVER_DOMAIN=localhost
GF_SERVER_HTTP_PORT=3000

# =============================================================================
# DATABASE (Optional - uses SQLite by default)
# =============================================================================
# Uncomment to use PostgreSQL instead of SQLite
# GF_DATABASE_TYPE=postgres
# GF_DATABASE_HOST=postgres:5432
# GF_DATABASE_NAME=grafana
# GF_DATABASE_USER=grafana
# GF_DATABASE_PASSWORD=CHANGE_ME_GRAFANA_DB_PASSWORD

# =============================================================================
# PLUGINS
# =============================================================================
# Comma-separated list of plugin IDs to install
# Example: grafana-piechart-panel,grafana-clock-panel
GF_INSTALL_PLUGINS=

# =============================================================================
# LOGGING
# =============================================================================
# Log level: debug, info, warn, error, critical
GF_LOG_LEVEL=info
GF_LOG_MODE=console

# =============================================================================
# SMTP (Optional - for email alerts)
# =============================================================================
# GF_SMTP_ENABLED=false
# GF_SMTP_HOST=smtp.gmail.com:587
# GF_SMTP_USER=your-email@gmail.com
# GF_SMTP_PASSWORD=your-app-password
# GF_SMTP_FROM_ADDRESS=grafana@yourdomain.com
# GF_SMTP_FROM_NAME=Grafana

# =============================================================================
# SECURITY HEADERS
# =============================================================================
# Enable security headers
GF_SECURITY_COOKIE_SECURE=false
GF_SECURITY_COOKIE_SAMESITE=lax
GF_SECURITY_STRICT_TRANSPORT_SECURITY=false

# =============================================================================
# ANALYTICS
# =============================================================================
# Disable usage analytics reporting to Grafana Labs
GF_ANALYTICS_REPORTING_ENABLED=false
GF_ANALYTICS_CHECK_FOR_UPDATES=true
