# =============================================================================
# Redis Configuration
# =============================================================================
# Copy this file to .env.redis and update with secure values
#
# NOTE: REDIS_PASSWORD is substituted from root .env via docker-compose
# This ensures password matches between redis container and backend service

REDIS_PASSWORD=${REDIS_PASSWORD}
