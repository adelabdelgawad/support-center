# =============================================================================
# SignalR Service Configuration (Example/Template)
# =============================================================================
# Copy this file to .env.signalr and update with your actual values
# Never commit .env files with real credentials to version control

# =============================================================================
# REDIS CONNECTION - REQUIRED
# =============================================================================
# Redis URL for SignalR backplane and Streams consumer
# Format: redis://:password@host:port/db
# Docker: Use redis:6379 (container name)
# Host dev: Use localhost:6380 (exposed port)
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

# =============================================================================
# AUTHENTICATION - REQUIRED
# =============================================================================
# Internal API key for backend-to-SignalR communication
# Must match SIGNALR_INTERNAL_API_KEY in backend .env
INTERNAL_API_KEY=${SIGNALR_INTERNAL_API_KEY}

# JWT secret key for token validation
# Must match SECURITY_JWT_SECRET_KEY in backend .env
JWT_SECRET_KEY=${JWT_SECRET_KEY}

# =============================================================================
# SIGNALR CONFIGURATION - OPTIONAL
# =============================================================================
# Enable Redis backplane for horizontal scaling (multiple SignalR instances)
# Set to true when running multiple SignalR replicas
EnableRedisBackplane=false

# Idempotency TTL in minutes (prevents duplicate event processing)
IdempotencyTtlMinutes=5

# =============================================================================
# LOGGING - OPTIONAL
# =============================================================================
# Log level (Trace, Debug, Information, Warning, Error, Critical)
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning
Logging__LogLevel__Microsoft.AspNetCore.SignalR=Debug
