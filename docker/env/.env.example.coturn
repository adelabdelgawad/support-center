# =============================================================================
# CoTURN Server Configuration
# =============================================================================
# Copy this file to .env.coturn and update with your actual values
# Used by Docker Compose for TURN server deployment (WebRTC NAT traversal)


# =============================================================================
# TURN REALM & AUTHENTICATION
# =============================================================================
# TURN realm - your domain name
TURN_REALM=example.com

# Shared secret for time-limited credentials (HMAC-SHA1)
# Generate using: openssl rand -hex 32
# NOTE: TURN_SECRET is substituted from root .env via docker-compose
# This ensures secret matches between coturn container and backend service
TURN_SECRET=${TURN_SECRET}


# =============================================================================
# NETWORK CONFIGURATION (set in docker-compose.yml or shell env)
# =============================================================================
# These are read by docker-compose.yml, not by coturn directly
# TURN_LISTENING_IP - Local IP to listen on (default: 0.0.0.0)
# TURN_RELAY_IP - IP for relaying media (default: 0.0.0.0)
# TURN_EXTERNAL_IP - Public IP / NAT mapping (format: external/internal or just external)
