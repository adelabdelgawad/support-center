# =============================================================================
# Frontend (Next.js) Configuration for Docker Deployment
# =============================================================================
# Copy this file to .env.frontend and update with your actual values
# NOTE: Docker uses internal networking (backend:8000)
#       For local development, use src/it-app/.env.local.example instead


# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================
# Server-side API URL (used by Next.js server components/API routes)
# In Docker, use container name; locally, use host IP/domain
NEXT_PUBLIC_API_URL=http://backend:8000
NEXT_PUBLIC_API_BASE_PATH=/api/v1

# =============================================================================
# SIGNALR CONFIGURATION (Real-time communication)
# =============================================================================
# SignalR hub URL for real-time updates (replaces WebSocket)
# Uses HTTP for negotiation - SignalR handles WebSocket upgrade internally
NEXT_PUBLIC_SIGNALR_URL=http://signalr-service:5000/hubs


# =============================================================================
# SESSION CONFIGURATION (server-side)
# =============================================================================
# NOTE: SESSION_SECRET is substituted from root .env via docker-compose
# Generate using: openssl rand -hex 32
SESSION_SECRET=${SESSION_SECRET}


# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# CRITICAL: Must match backend JWT_SECRET_KEY for token verification
# NOTE: JWT_SECRET_KEY is substituted from root .env via docker-compose
JWT_SECRET=${JWT_SECRET_KEY}

# Token expiration (should match backend settings)
ACCESS_TOKEN_EXPIRE_DAYS=30


# =============================================================================
# CORS / DEV ORIGINS (optional)
# =============================================================================
# Comma-separated list of allowed origins for development
ALLOWED_DEV_ORIGINS=localhost,backend
