# =============================================================================
# MinIO Object Storage Configuration
# =============================================================================
# Copy this file to .env.minio and update with secure values
#
# NOTE: MINIO_* variables are substituted from root .env via docker-compose
# This ensures credentials match between minio container and backend service

MINIO_ROOT_USER=${MINIO_ROOT_USER}
MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
