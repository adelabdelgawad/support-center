# =============================================================================
# MinIO Object Storage Configuration
# =============================================================================
# Copy this file to .env.minio and update with secure values
# Credentials MUST match MINIO_ACCESS_KEY and MINIO_SECRET_KEY in .env.backend

MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD
